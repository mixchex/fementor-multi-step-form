import styled from 'styled-components';

export const StyledToggleLabel = styled.div`
    & label {
        font-weight:500;
        margin:0 16px;
        cursor:pointer;
    }
    & p {
        font-weight:500;
        color:var(--marine-blue);
        margin:0 16px;

        
        cursor: not-allowed;
    }
`